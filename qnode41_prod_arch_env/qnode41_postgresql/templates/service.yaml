apiVersion: v1
kind: Service
metadata:
  name: {{ include "qnode41_postgresql.fullname" . }}
  labels:
    {{- include "qnode41_postgresql.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qnode41_postgresql.selectorLabels" . | nindent 4 }}
