apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qredis.fullname" . }}-test-connection"
  labels:
    {{- include "qredis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qredis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
