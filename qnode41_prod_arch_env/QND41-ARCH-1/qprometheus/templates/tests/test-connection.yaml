apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "qprometheus.fullname" . }}-test-connection"
  labels:
    {{- include "qprometheus.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "qprometheus.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
